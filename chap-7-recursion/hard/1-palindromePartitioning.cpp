#include<bits/stdc++.h>
using namespace std;

// given a string s, we have to partition it such that every substring of the partitioning is a palindrome and we have to return all such possible partitions

// the brute force would be generating all possible partitions, so we start from the first index and we can form a partition if the left substring is a palindrome, then we can check for right, but if the left substring is not palindrome then we dont have to check and straight up move ahead



int main() {
    
    return 0;
}