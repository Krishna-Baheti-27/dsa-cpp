#include<bits/stdc++.h>
using namespace std;

// we have to return the power set or set of all subsets (dont include duplicates which we already did if you see part 3)

int main() {
    
    return 0;
}