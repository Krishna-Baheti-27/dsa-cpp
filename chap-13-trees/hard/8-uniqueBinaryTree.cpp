#include<bits/stdc++.h>
using namespace std;

// in order to construct an unique Binary Tree you must have inorder traversal given along with any other traversal, but giving preorder and postorder is not sufficient to get the unique binary tree instead there will be a lot of trees having the same preorder and postorder

// inorder, postorder => unique bt
// inorder, preorder => unique bt
// postorder, preorder => not unique


int main() {
    
    return 0;
}