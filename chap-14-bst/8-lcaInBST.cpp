#include<bits/stdc++.h>
using namespace std;

class Node {
    public:
    int data;
    Node *left;
    Node *right;
    Node(int val) {
        data = val;
        left = right = nullptr;
    }
};

// the naive approach is using the same what we used in BT

// the optimal approach takes advan

Node* lcaInBST(Node *root) {

}

int main() {
    
    return 0;
}