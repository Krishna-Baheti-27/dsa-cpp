#include<bits/stdc++.h>
using namespace std;

// we have already solved this in array easy, longest subarray having given sum = k
// here we just replace k = 0
// but here array contains both negatives and positives and hence the optimal solution will involve hashing

int main() {
    
    return 0;
}