#include<bits/stdc++.h>
using namespace std;

// we can also use randomised quick sort which gurantees worst case time of nlogn, so worst case is not O(n^2) anymore, or use DNF algo to determine that

int main() {
    
    return 0;
}