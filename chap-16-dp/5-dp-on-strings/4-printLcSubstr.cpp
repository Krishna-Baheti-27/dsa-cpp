#include <bits/stdc++.h>
using namespace std;

// similar to the previous problem we will build the dp table and in the process keep the streak of the characters matching and the max we get is our longest common substring if we get a mismatch in the middle we again restart from 0 and hence maintain the max strign variable for that

int main() {
    
    return 0;
}