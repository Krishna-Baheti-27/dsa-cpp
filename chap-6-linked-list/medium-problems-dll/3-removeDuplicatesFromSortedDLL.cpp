#include<bits/stdc++.h>
using namespace std;

class Node {
    public:
    int data;
    Node *next;
    Node *prev;
    Node(int data) {
        this->data = data;
        next = nullptr;
        prev = nullptr;
    }
};

// you have to remove duplicated and return head of dll having only unique elements

Node *removeDuplicates(Node *head) {
    
}

int main() {
    
    return 0;
}