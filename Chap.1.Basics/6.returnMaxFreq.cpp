#include<bits/stdc++.h>
using namespace std;

int maxFreqBrute(vector<int> &arr, int k) {
    int maxFreq = 1;
    sort(arr.begin(), arr.end());
    // we can only make smaller elements equal to some larger oe since we can only decrement
    
}



int main() {
    
    return 0;
}