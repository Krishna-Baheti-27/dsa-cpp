#include<bits/stdc++.h>
using namespace std;

// this is with context to weighted, undirected and connected graph

// A spanning tree of a graph is a tree having n nodes, n - 1 edges where all nodes are connected to each other with no cycles (this is implied by the definition of tree itself), where n is number of nodes in the original graph

// MST is minimum spanning tree or minimum cost spanning tree is the tree having least sum of all the edge weights of the tree and that is the MST for the given graph

int main() {
    
    return 0;
}