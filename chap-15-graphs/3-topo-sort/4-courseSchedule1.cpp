#include<bits/stdc++.h>
using namespace std;

// this questions is basically of if the graph is directed acyclic then we can finish else cant

// numCourses = 2, prerequisites = [[1,0],[0,1]]
// here 1 is pointing to 0, for the first element which is fine but for the second element we 0 pointing to 1 which is cycle so not allowed
// so we have been basically given the edges construct the graph using it and detect cycle

bool canFinish(int numCourses, vector<vector<int>> &prerequisites) {

}

int main() {
    
    return 0;
}