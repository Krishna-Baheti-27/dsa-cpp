#include<bits/stdc++.h>
using namespace std;

// connected components are basically individual disconnected portions of single graph which are not directly connected but all of these parts are still part of a single graph and to traverse across them we maintain a visited array so make sure we complete traversal on all the nodes

int main() {
    
    return 0;
}