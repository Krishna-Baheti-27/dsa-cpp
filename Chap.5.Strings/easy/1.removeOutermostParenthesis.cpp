#include<bits/stdc++.h>
using namespace std;

string removeParenBrute(string s) {
    // first break the string into primitves, then remove outer paren and then concatenate them
    for(int i = 0; i < s.size(); i++) {
        if(s[i] == '(') {
            
        }
    }
}

int main() {
    
    return 0;
}