#include <bits/stdc++.h>
using namespace std;

// Given an unsorted array, arr[] of positive numbers that contains even number of occurrences for all numbers except two numbers. Return that two numbers in decreasing order which has odd occurrences.

//////////////////////////////////////////////////////////////////////////////////////

// the brute force is to use a map to store the frequencies of element and return the onlytwo numbers having odd frequency

//////////////////////////////////////////////////////////////////////////////////////

// but the optimal solution is take xor of all numbers, this would cancel out all the even occurence numbers and would leave us with the xor of our numbers, but we now have to find the two numbers out of our xor

vector<int> twoOddNum(vector<int> &arr) {
     
}

int main() {
    
    return 0;
}